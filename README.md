e projet nécessite l'installation de certaines dépendances Node.js avant de pouvoir exécuter les tests.
⚙️ Installer les dépendances manquantes

Exécutez la commande suivante dans votre terminal :

npm install winston validator express-mongo-sanitize

Une fois les dépendances installées, vous pouvez lancer les tests spécifiques au dossier utils avec la commande :

npm test -- src/tests/utils/

📌 Assurez-vous d'avoir Node.js et npm installés sur votre machine avant de commencer.